Task.md
.metadata
.codeartsdoer
.zcode

# ---- STM32CubeMX / CubeIDE 构建产物（可随时由 IDE 重建）----
Debug/
Release/
build/
*.o
*.d
*.elf
*.map
*.lst
*.su
*.cyclo
*.ioc.bak

# others
code.txt
error.txt
key.txt

# ---- local build/run artifacts + stale copies (2026-09-11) ----
*.exe
__pycache__/
core1_ui/deploy/
core1_ui/rpmsg_link_test.py

# ---- CA bundle copied from the dev PC for the board (/etc/park/ca.pem) ----
# public Mozilla certs, but a local copy: the board image has no /etc/ssl/certs
core1_ui/ca.pem
core1_ui/ca-bundle.crt

# ---- step 7: cloud settings hold the API key, never commit them (P7-07) ----
cloud.conf
cloud.conf.bak
cloud.env
**/cloud.conf

# ---- other live runtime configs: only the sample_* copies are tracked ----
# (WiFi PSK, board overrides and the core0 daemon config live on the board)
core0.conf
**/core0.conf
wpa_supplicant.conf
**/wpa_supplicant.conf
park-ui.env
**/park-ui.env

# never ignore the templates themselves
!sample_*
!**/sample_*
!*.example
