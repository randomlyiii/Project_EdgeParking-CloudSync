# 验收 KPI 与测试用例总表

> 支撑文档：第8步 P8-01 的实测依据。目标值源自 `Task.md` 第十节（初始目标，实测后修订）。
> 用法：每项 KPI 给出**测量方法**，实测后填"实测值/结论"；超标项回到对应步骤文档整改再回归。

## 1. KPI 总表

| # | 指标 | 目标 | 测量方法（用例） | 实测值 | 结论 |
|---|---|---|---|---|---|
| K1 | 触发闭环延迟（遮光→开闸指令发出，无云） | ≤3s | T0~T6 埋点（P6-05），10 次取均值+最大值 | | |
| K2 | 含云兜底闭环延迟 | 云段限 5s，整体硬上限 6s | 同上，低置信样本 10 次，单列云段耗时（P7-04 延长窗策略：`cloud_pending` 时 Core0 超时 3s→6s） | | |
| K3 | rpmsg 断链检出 | ≤1s | remoteproc stop M4，日志时间差 ×5 次 | | |
| K4 | rpmsg 恢复状态重同步 | 不丢业务状态 | 断链期间遮光 1 次，恢复后闸/到位状态正确 | | |
| K5 | K210 端侧识别准确率（白天） | ≥90% | 50 张车牌样本（打印牌+实车），路径 B 改测检出率 | | |
| K6 | 云兜底成功率 | 优于端侧 | 同批样本中端侧失败/低置信的子集，云端成功率对比 | | |
| K7 | 单次识别耗时（事件触发，端侧） | ≤3s | 0xC1 发出→0xC2/C3 回到，日志差 ×10 | | |
| K8 | 预览流帧率 | ≥5fps | Core1 收帧计数 60s | | |
| K9 | 预览质量 | 无花屏 | 目视 + 抽帧存文件检查 ×10 帧 | | |
| K10 | 识别运行不影响预览 | 是 | 识别触发前后 60s 帧率对比（降幅 <20% 且自恢复） | | |
| K11 | 状态刷新 Core0→UI | ≤500ms | Modbus 写触发状态变更→UI 变化，高频相机或日志差 ×10 | | |
| K12 | 进程崩溃自恢复 | 是 | kill core1/Qt → ≤5s 拉起、Core0 控制链路不断 ×3 | | |
| K13 | 遮光误触发率 | 可接受 | 24h 长稳（P8-07）计数：误触发次数/天 | | |
| K14 | Modbus 响应 | ≤50ms | Modbus Poll 循环读 0x0000×100 均值 | | |
| K15 | 冷启动自愈 | 3/3 | 断电重启 3 次全自动到工作态 | | |

## 2. 分步验收门与 KPI 对应

| 门 | 内容速览 | 关联 KPI |
|---|---|---|
| G0 | 环境/物料/接线（60Ω、共地） | — |
| G1 | C8T6 本地：OLED/遮光状态机/SG90 自检 | K13 前置 |
| G2 | CAN 双向 500k：心跳/事件/指令 + 错误计数 0 | K13 前置 |
| G3 | RPMSG：断链 ≤1s、恢复重同步 | K3/K4 |
| G4 | 本地业务：超时降级、白名单、Modbus | K14 |
| G5 | K210：预览 ≥5fps、一次触发一次结果 | K5/K7/K8/K9/K10 |
| G6 | 本地全闭环 ≤3s、UI ≤500ms、压测达标 | K1/K11 + 压测 |
| G7 | 云兜底 5s 超时、回退、云故障本地照常 | K2/K6 |
| G8 | 全链路 + 自启 + 看门狗 + 24h 长稳 = 交付 | K12/K13/K15 |

## 3. 场景用例（G6/G8 复用）

| 用例 | 步骤 | 期望 |
|---|---|---|
| S1 白名单车牌 + 光照清晰 | P6-06 | 自动开闸，K1 达标 |
| S2 非白名单车牌 | P6-06 | DENY，不开闸，有记录，UI 红卡 |
| S3 低置信样本（无云） | P6-06 | 超时/失败分支 → 降级提示 |
| S4 低置信样本（有云） | P7-06 | 自动云兜底成功开闸，K2 记录 |
| S5 遮光 20 连测 | P2-16/P6-06 | 无重复开闸（冷却）、无误触发 |
| S6 拔 K210 串口 | P8-03 | 预览断、识别线不可用、降级通道可用 |
| S7 断外网 | P8-03 | 云 5s 失败回退；本地照常 |
| S8 kill core1 | P8-03 | ≤3s 故障字、≤5s 拉起 |
| S9 拔 CAN 线 | P8-03 | 3s 判离线上报；插回恢复 |
| S10 断电冷启动 | P8-04 | 3/3 自动全通 |
| S11 24h 长稳 | P8-07 | 无干预存活、误触发可接受、无泄漏趋势 |

## 4. 记录约定

- 每次实测记：日期、环境（光照/温度）、样本量、原始数据存放路径（日志片段）；
- 达标即在本文与对应步骤文档双向打钩；改阈值（`SHADE_DROP_PERCENT`、`RECOG_CONF_TH` 等）必须记录新旧值与理由 → 同步 AGENTS.md。
